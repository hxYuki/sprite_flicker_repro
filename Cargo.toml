[package]
name = "sprite_flicker"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { git = "https://github.com/bevyengine/bevy", rev = "671eca5", default-features = false, features = [
  "2d",
] }
